<div class="applicant-line">
    <img src="{{ application.avatar_image }}" alt="profile">
    <div class="applicant-info">
      <span>{{ application.firstName }}</span>
      <p>Applied for <strong>{{ application.course_name }}</strong></p>
    </div>
</div>